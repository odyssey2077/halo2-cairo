Cairo 0 example files and associated `json` artifacts as produced by https://cairo-lang.org/playground/

To generate `res.json`:
* hit `Debug` on the playground
* find the associated `/cairo` HTTP call in your browser dev console
* copy/paste the HTTP call answer